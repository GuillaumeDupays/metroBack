import { PositionsUtilisateurs } from './positions_utilisateurs';

describe('PositionsUtilisateurs', () => {
  it('should be defined', () => {
    expect(new PositionsUtilisateurs()).toBeDefined();
  });
});
